# Server-side Atlantis config — this is the actual switch that makes
# `atlantis apply` refuse to run until the PR is approved and mergeable.
repos:
  - id: github.com/ShantanuMakkar/Platform-pulse-infra-project
    apply_requirements: [mergeable]
    allowed_overrides: [apply_requirements, workflow]
    allow_custom_workflows: true
